# Upskillink frontend

Next.js App Router implementation of the production Figma website.

```bash
npm install
npm run dev
```

Quality checks:

```bash
npm run lint
npx tsc --noEmit
npm run build
```

The Figma inventory and resumable build checkpoint live at the repository root.
